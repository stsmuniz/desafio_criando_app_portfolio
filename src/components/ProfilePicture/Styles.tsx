import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  profilePic: {
    width: 150,
    height: 150,
    borderRadius: 75,
    borderWidth: 5,
    borderColor: '#dadada',
  },
});