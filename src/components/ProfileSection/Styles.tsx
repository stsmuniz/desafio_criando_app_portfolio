import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  profilePicContainer: {
    alignItems: 'center',
  },
  nameContainer: {
    alignItems: 'center',
  },
  name: {
    fontSize: 24,
    fontWeight: 'bold',
    marginTop: 8,
  },
  bio: {
    fontSize: 16,
    color: '#666',
  },
});